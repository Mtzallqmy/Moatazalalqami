<?xml version="1.0" encoding="utf-8"?>
<!--
  Release traffic is TLS-only by default. Cleartext is limited to loopback and mDNS-style
  .local hosts used by the embedded Linux runtime and explicitly local MCP/LLM services.
  Public provider endpoints, arbitrary browser hosts, and raw LAN IPs must use HTTPS.
-->
<network-security-config>
  <base-config cleartextTrafficPermitted="false">
    <trust-anchors>
      <certificates src="system" />
    </trust-anchors>
  </base-config>
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="false">localhost</domain>
    <domain includeSubdomains="false">127.0.0.1</domain>
    <domain includeSubdomains="true">local</domain>
  </domain-config>
</network-security-config>
